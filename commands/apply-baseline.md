---
description: Apply Cursor hardening baseline in current project.
---

Run baseline setup for the current project:

1. Run `python scripts/apply_baseline.py`.
2. Show command output.
3. If baseline files were created, list which ones were added.
