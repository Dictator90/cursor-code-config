---
description: Add sessionStart handoff-check hook to Cursor hooks config.
---

Add the `sessionStart` handoff-check hook for this repository.

Steps:
1. Run `python scripts/add_hook.py session-handoff-check`.
2. Show the command output.
3. Confirm which hooks config file was updated.
