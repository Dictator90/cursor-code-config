---
description: Run strict config drift validation for current project.
---

Run strict drift validation:

1. Run `python scripts/validate_config.py --strict`.
2. Show output.
3. If drift is detected, point to `.cursor/drift-report.md`.
